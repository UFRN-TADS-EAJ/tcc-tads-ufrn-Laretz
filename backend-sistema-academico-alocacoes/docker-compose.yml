
version: '3.8'

services:
  db:
    image: postgres:15
    container_name: postgresbd
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: pgpassword
      POSTGRES_DB: postgresbd
    ports:
      - "5432:5432"