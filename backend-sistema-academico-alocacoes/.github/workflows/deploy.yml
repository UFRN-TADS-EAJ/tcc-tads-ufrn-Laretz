name: Deploy Backend

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: deploy-backend-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-artifact:
    name: Build Backend and Upload Artifact
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: ./backend

      - name: Build backend
        run: npm run build
        working-directory: ./backend

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: backend-dist
          path: |
            backend/dist
            backend/package.json
            backend/package-lock.json
            backend/prisma

  build-and-push-image:
    name: Build and Push Docker Image (GHCR)
    runs-on: ubuntu-latest
    needs: build-artifact
    if: ${{ hashFiles('backend/Dockerfile') != '' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          push: true
          tags: |
            ghcr.io/${{ github.repository }}-backend:latest
            ghcr.io/${{ github.repository }}-backend:${{ github.sha }}

  # Exemplo (opcional) de job de deploy via SSH + Docker Compose em servidor próprio.
  # Ajuste conforme sua infraestrutura e descomente quando tiver pronto.
  # deploy-ssh:
  #   name: Deploy to Server (SSH)
  #   runs-on: ubuntu-latest
  #   needs: build-and-push-image
  #   if: ${{ hashFiles('backend/Dockerfile') != '' }}
  #   environment: production
  #   steps:
  #     - name: SSH and deploy
  #       uses: appleboy/ssh-action@v0.1.10
  #       with:
  #         host: ${{ secrets.SSH_HOST }}
  #         username: ${{ secrets.SSH_USER }}
  #         key: ${{ secrets.SSH_KEY }}
  #         script: |
  #           docker login ghcr.io -u ${{ github.actor }} -p ${{ secrets.GITHUB_TOKEN }}
  #           docker pull ghcr.io/${{ github.repository }}-backend:latest
  #           docker compose -f /path/to/docker-compose.yml up -d --force-recreate